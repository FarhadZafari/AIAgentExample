# Core web framework
Flask==3.0.3

# Flask internals (usually installed automatically with Flask, but pinned here for reproducibility)
Werkzeug==3.0.3
Jinja2==3.1.4
Click==8.1.7
itsdangerous==2.2.0

# Production server (optional, not needed for local dev)
gunicorn==22.0.0

# If frontend is served on a different domain/port
Flask-Cors==4.0.0
langgraph
langchain_openai
